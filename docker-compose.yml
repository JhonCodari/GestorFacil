services:
  db:
    image: postgres:16
    container_name: gestorfacil_db
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${POSTGRES_USER} -d ${POSTGRES_DB}"]
      interval: 10s
      timeout: 5s
      retries: 5
    environment:
      POSTGRES_DB: ${POSTGRES_DB}
      POSTGRES_USER: ${POSTGRES_USER}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD}
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    restart: always

  redis:
    image: redis:7
    container_name: gestorfacil_redis
    ports:
      - "6379:6379"
    restart: always
  
  app:
    build: .
    container_name: gestorfacil-app
    depends_on:
      db:
        condition: service_healthy
      redis:
        condition: service_started
        
    ports:
      - "8080:8080"
      - "5005:5005"
    environment:
      SPRING_PROFILES_ACTIVE: docker
      SPRING_SECURITY_USER_NAME: ${SPRING_SECURITY_USER_NAME}
      SPRING_SECURITY_USER_PASSWORD: ${SPRING_SECURITY_PASSWORD}
      JWT_SECRET: ${JWT_SECRET}

  wiremock:
    image: wiremock/wiremock:3.10.0
    container_name: banco-conta_wiremock
    ports:
      - "8090:8080"
    volumes:
      - ./src/main/resources/templates/mappings:/home/wiremock/mappings
      - ./src/main/resources/templates/__files:/home/wiremock/__files
    command: --global-response-templating --verbose

volumes:
  postgres_data:

